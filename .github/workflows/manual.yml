name: Manual workflow

on:
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
    - name: Send greeting
      env:
        GITHUB_TOKEN: ${{ secrets.Nothing }}  # Example of using a GitHub secret
      run: echo "Hello World"
