name: Manual workflow

on:
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Send greeting
        run: echo "Not Hello World ${{secrets.GITHUB_TOKEN}}"
      - name: Get secret
        run: echo "Not ..... ...... ${{secrets.SAIFI_ACTION}}"
      - name: Get Variable
        run: echo "Variable Value : ${{vars.DEMOVARIABLE}}"
